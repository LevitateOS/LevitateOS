[package]
name = "vendor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Vendor management for LevitateOS external sources"

[[bin]]
name = "vendor"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[lints]
workspace = true
